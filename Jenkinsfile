pipeline {
    agent { dockerfile true }
    stages {
        stage ('test stage'){
            steps {
                sh 'node -v'
                echo 'jenkins working !'
            }
        }
    }
}